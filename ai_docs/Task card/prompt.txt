I want you to create me a task card like in the following image.
Note that the image is made in paint, so it is not styled. I want you to make it pretty.

It is supposed to be part a list of cards, so i will describe the requested behavior:

The card has two states, collapsed and expanded:
The collapsed state shows only the title and the 3 dots
The expanded state shows also a container that expands from the bottom to reveal the description, the due date and the mark as done button.

Clicking on the three buttons should make a pop-up menu with the described buttons

Make the task card a reusable composable whose only argument is a Task object